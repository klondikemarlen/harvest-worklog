{
  "name": "harvest-worklog",
  "version": "0.13.13",
  "description": "Project Time work-timesheet drafts with manual Harvest writes.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "npm run test:omp",
    "test:omp": "node --test test/*.test.js",
    "release": "bin/release-plugin",
    "verify:release": "bin/verify-plugin-release"
  },
  "keywords": [
    "omp",
    "oh-my-pi",
    "plugin",
    "harvest",
    "time-tracking"
  ],
  "omp": {
    "extensions": [
      "./index.js"
    ],
    "settings": {
      "defaultHours": {
        "type": "number",
        "default": 7,
        "min": 0.01,
        "description": "Hours per business day when the Harvest tool call omits hours."
      },
      "holidayRegions": {
        "type": "string",
        "default": "ca_yt",
        "description": "Comma-separated Holidays regions; defaults to Yukon, Canada."
      },
      "command": {
        "type": "string",
        "default": "harvest-worklog",
        "description": "Direct path to the harvest-worklog executable; credentials stay in HARVEST_ACCESS_TOKEN and HARVEST_ACCOUNT_ID."
      },
      "projectTimeMappings": {
        "type": "string",
        "default": "{}",
        "description": "Optional JSON mapping from OMP Project Time project names to Harvest project and task names."
      },
      "projectTimeLogPath": {
        "type": "string",
        "default": "",
        "description": "Optional Project Time evidence path; defaults to ~/.omp/project-time/time-log.sqlite. A legacy v1 JSON snapshot is also supported."
      }
    }
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
